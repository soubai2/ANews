# 新闻推送 Agent 规范

## 功能概述

新闻推送 Agent 是一个以 App 内操作为中心的新闻获取、排序、推送与跟进系统。Agent 负责定时抓取新闻、维护每日新闻池、根据用户偏好和主流热度判断新闻重要性，并在 App 内的推送页面和对话页面中呈现结果。

核心能力包括：

- 每隔 2 小时自动更新一次新闻推送。
- 仅抓取上一次推送时间到当前时间之间发布的新新闻，按新闻发布时间判断，允许约 10 分钟时间误差。
- 将最新抓取到的新闻放入“最新”板块，并同步加入当天新闻池。
- 在“相关”板块中按新闻重要性排序，排序依据包括用户偏好、新闻热度、来源权威性和事件影响范围。
- 对用户点击“跟进”的新闻持续追踪后续进展，有更新时推送到“跟进”板块。
- 支持用户指定新闻网站、行业媒体、公司官网或其他可信来源，Agent 在每次推送时必须纳入这些来源。
- 所有阅读、关注、跟进、偏好管理和主动查询都在 App 内完成，用户无需跳转到外部浏览器。

## 技术栈推荐（Windows 桌面版）

### 前端与 UI

- 推荐使用 Electron 构建 Windows 桌面 App，通过 HTML、CSS 和 JavaScript 实现跨平台桌面界面。
- Electron 支持内嵌网页容器，可用于实现新闻点击后在 App 内打开原网页，不产生独立浏览器窗口。
- 前端框架可使用 React，并与 Electron 结合构建推送页面、对话页面、新闻详情页和偏好管理界面。
- 如未来需要复用 Web 端逻辑，可考虑 React Native Web + Electron 的组合，但桌面首版建议优先保持 React + Electron 的简单结构。

### 后端与任务调度

- 推荐使用 Python + FastAPI 作为本地后端服务，负责新闻抓取、清洗、摘要生成、重要性评分、用户偏好管理和跟进任务管理。
- 定时任务建议使用 APScheduler，在 Windows 桌面环境中每隔 2 小时触发一次新闻抓取和推送流程。
- 如项目初期功能较轻，也可使用 Python 内置 `schedule` 库实现简单定时任务；当任务数量、失败重试和状态管理变复杂时，应切换到 APScheduler。
- Electron 前端与 FastAPI 后端可通过本地 HTTP API 或本地 IPC 桥接进行通信。

### 数据库与缓存

- 单用户 Windows 桌面版推荐使用 SQLite，部署简单、无独立数据库服务依赖，适合存储新闻池、用户偏好、跟进列表、指定来源和推送状态。
- 如果未来演进为多用户服务端版本，可切换或扩展到 PostgreSQL。
- Redis 为可选组件，可用于缓存新闻抓取结果、临时推送状态、网页解析结果和热点统计；桌面首版不应强依赖 Redis。

### AI 与新闻处理

- 可调用 GPT 模型处理新闻摘要、主题提取、实体识别、重要性评分和推荐原因生成。
- 模型来源可选择 OpenAI API，也可根据隐私、成本和离线需求接入本地小型模型。
- 向量数据库为可选组件，可用于存储用户兴趣向量、新闻语义向量和相似新闻检索结果，从而提升个性化排序和新闻去重能力。
- 首版可先使用结构化偏好标签和关键词权重实现排序，后续再引入向量检索增强推荐质量。

### 推送与通知

- 推荐使用 Electron 自带桌面通知 API 弹出最新新闻提醒。
- 推送通知应只展示摘要信息，用户点击通知后回到 App 内的对应新闻或推送页面。
- “关注”和“跟进”按钮应直接更新本地数据库和用户偏好设置，并立即影响后续推送排序。
- 所有通知、新闻阅读、偏好编辑和跟进管理都应保持在 App 内完成。

## 新闻抓取策略

### 抓取时间窗口

- Agent 记录每次成功推送的时间 `last_push_at`。
- 每次抓取时，以当前时间 `now` 为结束时间，仅抓取发布时间在以下窗口内的新闻：
  - 开始时间：`last_push_at - 10 分钟`
  - 结束时间：`now + 10 分钟`
- 10 分钟容错用于处理来源站点发布时间延迟、时区标注差异、RSS 更新时间和页面发布时间不一致等问题。
- 对容错窗口内重复出现的新闻，必须通过 URL、标题、来源、发布时间和正文摘要进行去重，避免重复推送。

### 抓取来源

Agent 的新闻来源分为默认来源和用户指定来源：

- 默认来源：主流新闻网站、行业媒体、财经科技媒体、公开 RSS、可访问的搜索索引和可信新闻聚合源。
- 用户指定来源：用户在对话页面中指定的新闻网站、公司官网、公告页、博客、投资者关系页面、产品更新页或监管披露页面。
- 用户指定来源具有高优先级，每次定时推送都必须抓取。
- 如果用户指定来源暂时不可访问，Agent 应记录失败状态，并在下一轮推送中重试。

### 新闻入库与分类

每条新闻进入新闻池前应完成标准化处理：

- 提取标题、来源、原始 URL、发布时间、抓取时间、摘要、正文要点、主题标签和涉及实体。
- 识别新闻类别，例如国际、国内、财经、科技、公司、行业、政策、产品、安全、突发事件等。
- 识别涉及公司、人物、地点、行业、产品、股票代码或项目名称。
- 生成新闻唯一标识，用于去重、跟进和后续更新关联。
- 将新闻写入当天新闻池，并按日期归档。

## 推送逻辑

### 最新板块

“最新”板块展示本轮抓取到的新增新闻。

- 只展示自上一次推送以来的新新闻。
- 默认按发布时间倒序排列。
- 对突发、重大、强相关或用户指定来源新闻可适当提前展示。
- 每条新闻应展示标题、来源、发布时间、摘要、分类标签、重要性标记和操作按钮。

### 相关板块

“相关”板块展示 Agent 判断与用户偏好最相关、最值得关注的新闻。

- 数据来源为当天新闻池，以及必要时最近若干天的高相关新闻。
- 排序主要依据新闻重要性分数。
- 用户点击“关注”的新闻类型、主题、来源和实体会进入用户偏好 prompt，并影响后续排序。
- 主流关注度高但用户尚未明确关注的新闻，也应保留一定曝光权重，避免信息茧房。

### 跟进板块

“跟进”板块展示用户主动要求追踪的新闻后续进展。

- 用户点击某条新闻的“跟进”后，Agent 将该新闻加入跟进列表。
- Agent 在每轮推送中检查该事件是否有新增报道、官方公告、数据更新、回应、澄清或结果。
- 若发现有效更新，应推送到“跟进”板块，并关联原新闻。
- 跟进内容应突出“发生了什么变化”，避免重复展示无新增信息的转载内容。

### 去重与合并

- 同一事件的多篇报道应合并为一个新闻主题，保留多个来源链接。
- 同标题、同 URL、同来源的新闻不得重复推送。
- 对不同来源报道同一事件的情况，Agent 应优先展示权威来源和信息量更高的版本。
- 对有新增事实的后续报道，应作为更新处理，而不是简单重复。

## 用户交互设计

### 新闻点击

- 用户点击新闻后，App 使用内置 WebView 或 App 内网页容器打开原文。
- 不得创建独立浏览器窗口。
- 页面顶部应保留 App 导航能力，使用户可返回推送页面或进入对话页面。
- 若原网页无法在 App 内正常展示，应提供 App 内错误提示和可重试操作。

### 关注按钮

每条新闻提供“关注”按钮，用于表达用户对该类新闻的长期兴趣。

点击“关注”后，Agent 应执行以下行为：

- 将新闻主题、分类、来源、涉及实体和关键词加入用户偏好 prompt。
- 提升相似新闻在“相关”板块中的排序权重。
- 记录关注时间和触发来源，便于后续解释推荐原因。
- 如果用户反复关注同类内容，应增强该偏好的权重。
- 用户应能在对话页面中查看、编辑或删除偏好。

### 跟进按钮

每条新闻提供“跟进”按钮，用于追踪特定事件后续。

点击“跟进”后，Agent 应执行以下行为：

- 将新闻加入跟进列表。
- 建立事件跟踪对象，包括原新闻、核心实体、关键词、时间线和相关来源。
- 每次定时推送时检查该事件是否有实质性更新。
- 有更新时推送到“跟进”板块。
- 用户可在对话页面中取消跟进。

### 指定来源

用户可在对话页面中指定要查询和持续抓取的网站。

支持的来源类型包括：

- 新闻网站首页、栏目页或 RSS 地址。
- 公司官网、新闻中心、博客、公告页或投资者关系页面。
- 政府、监管机构、交易所、研究机构或开源项目公告页。

Agent 对指定来源的行为规范：

- 每轮推送必须尝试抓取用户指定来源。
- 抓取失败时记录错误原因和最近成功时间。
- 对指定来源的新内容给予较高基础权重。
- 如果来源长期不可访问，应在对话页面中提示用户检查地址。

## 页面结构说明

### 推送页面

推送页面是用户接收新闻的主入口，应包含以下板块：

#### 最新

- 展示本轮新增新闻。
- 以时间顺序为主，兼顾重大新闻优先展示。
- 适合用户快速了解最近 2 小时内发生的新事件。

#### 相关

- 展示与用户偏好、关注主题和主流热点高度相关的新闻。
- 按重要性排序。
- 每条新闻应展示推荐原因，例如“与你关注的公司相关”“来自你指定的网站”“主流媒体高关注”。

#### 跟进

- 展示用户已选择跟进事件的后续更新。
- 每条更新应关联原始新闻和事件时间线。
- 优先展示有实质变化的内容，例如官方回应、调查结果、财务数据、产品发布、事故进展或政策落地。

### 对话页面

对话页面用于用户主动查询新闻和管理 Agent 行为。

应支持以下操作：

- 主动查询新闻，例如“今天有哪些 AI 芯片新闻？”“帮我查某公司的最新公告”。
- 添加、查看、编辑和删除用户偏好。
- 查看已关注的主题、来源、公司和关键词。
- 添加、查看或删除指定抓取网站。
- 查看、管理和取消跟进新闻。
- 询问推荐原因，例如“为什么把这条新闻排在前面？”。
- 调整推送偏好，例如减少娱乐新闻、增加公司公告、优先看英文来源等。

## 新闻重要性判断逻辑

Agent 应综合用户偏好和新闻热度计算新闻重要性。重要性分数不应只依赖单一指标，而应由多个维度加权得出。

建议评分维度如下：

- 用户偏好匹配度：是否命中用户关注的主题、行业、公司、人物、地区、来源或关键词。
- 主流关注度：是否被多个主流媒体、行业媒体或权威来源报道。
- 来源权威性：来源是否为官网、监管机构、主流媒体、专业媒体或一手公告。
- 事件影响范围：是否影响较大人群、市场、行业、政策、公司战略或公共安全。
- 新颖性：是否为首次报道、重大进展或相较已有新闻出现新的关键事实。
- 时效性：发布时间是否足够新，是否属于正在快速发展的事件。
- 跟进价值：是否可能产生后续更新，是否值得持续追踪。
- 用户历史行为：用户过去是否阅读、关注、跟进或忽略相似新闻。

重要性排序建议：

```text
重要性分数 =
  用户偏好匹配度
  + 主流关注度
  + 来源权威性
  + 事件影响范围
  + 新颖性
  + 时效性
  + 跟进价值
  + 用户历史行为权重
```

行为规范：

- 用户明确关注的主题应提升排序，但不能完全排除高热度重大新闻。
- 来自用户指定来源的新内容应获得额外权重。
- 对重复转载、标题党、缺乏新信息的内容应降低权重。
- 对突发事件、政策变化、重大公司公告、安全事故和市场异动应提高权重。
- 对敏感或高风险新闻，应优先展示权威来源，并标注信息状态，例如“初步报道”“官方确认”“等待进一步核实”。

## 推送更新频率与策略

### 更新频率

- Agent 每隔 2 小时执行一次新闻抓取、入库、排序和推送。
- 每轮推送完成后更新 `last_push_at`。
- 如果某轮抓取失败，不应错误推进 `last_push_at`；下一轮应从上一次成功推送时间继续抓取。
- 对突发重大新闻，可在 2 小时周期之外触发即时推送，但即时推送不得破坏常规定时窗口和去重逻辑。

### 推送策略

每轮推送应执行以下流程：

1. 读取 `last_push_at`、用户偏好、跟进列表和用户指定来源。
2. 按时间窗口抓取默认来源和用户指定来源的新新闻。
3. 清洗新闻数据，提取发布时间、来源、摘要、标签和涉及实体。
4. 去重并合并同一事件的多来源报道。
5. 将新新闻写入当天新闻池。
6. 更新“最新”板块。
7. 根据用户偏好和主流热度计算重要性分数，更新“相关”板块。
8. 检查跟进列表，对有实质更新的事件更新“跟进”板块。
9. 在 App 内生成推送结果。
10. 推送成功后记录本轮状态，并更新 `last_push_at`。

### 新闻池管理

- 每天建立独立新闻池，按用户本地日期归档。
- 新闻池保留当天所有抓取到的新闻，包括未展示在推送页面中的低优先级新闻。
- 每条新闻应记录分类、标签、来源、发布时间、抓取时间、重要性分数、是否已推送、是否被关注、是否被跟进。
- 新闻池支持对话页面查询，例如按日期、来源、公司、主题或关键词检索。
- 历史新闻池可用于生成趋势、事件时间线和推荐解释。

### App 内优先原则

- 新闻阅读在 App 内打开。
- 偏好管理在 App 内完成。
- 跟进管理在 App 内完成。
- 来源管理在 App 内完成。
- 用户无需离开 App 即可完成新闻获取、阅读、关注、跟进、查询和管理。
